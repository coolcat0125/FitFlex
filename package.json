{
  "name": "fitflex",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "轻量级健身动作查询工具 · 按肌群/器械/类型快速检索",
  "scripts": {
    "build": "tsc --noEmit && node tools/build.mjs",
    "typecheck": "tsc --noEmit",
    "preview": "node tools/serve.mjs",
    "data": "node -e \"console.log('数据处理见 tools/*.py，需 Python 3.13')\""
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}
